#ifndef OVERLAP_H
#define OVERLAP_H

#include <SFML/Graphics.hpp>
/*"bool" is overlab
it turns true when it overlabs, if not then it turns false*/
bool overlap(sf::Sprite& sprite1, sf::Sprite& sprite2);

#endif